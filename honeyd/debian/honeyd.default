# Defaults for honeyd initscript
# sourced by /etc/init.d/honeyd
# installed at /etc/default/honeyd by the maintainer scripts
NO_START=1

# Additional options that are passed to the Daemon.
DAEMON_OPTS=""
